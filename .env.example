# Database
# PostgreSQL database connection string (e.g., from Neon, Supabase, or local PostgreSQL)
# Format: postgresql://username:password@host:port/database?sslmode=require
DATABASE_URL=postgresql://username:password@host:port/database?sslmode=require

# Authentication (Better Auth)
# Generate a random secret key (32+ characters recommended)
# You can generate one with: openssl rand -base64 32
BETTER_AUTH_SECRET=your-random-secret-key-here-min-32-chars

# Base URL for authentication callbacks
# Production: https://yourdomain.com
# Development: http://localhost:3000
BETTER_AUTH_URL=http://localhost:3000

# Public App URL
# Used for client-side redirects and public links
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Admin Credentials
# Email for admin login
ADMIN_EMAIL=admin@yourdomain.com

# Admin password (change this immediately!)
# Use a strong password in production
ADMIN_PASSWORD=change-this-to-strong-password

# Google OAuth (Optional - for Google Sign-In)
# Get credentials from: https://console.cloud.google.com/apis/credentials
# Create OAuth 2.0 Client ID and enable Google+ API
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret

# Push Notifications (Web Push / VAPID)
# Generate with: pnpm exec web-push generate-vapid-keys
VAPID_PUBLIC_KEY=your-vapid-public-key
VAPID_PRIVATE_KEY=your-vapid-private-key
NEXT_PUBLIC_VAPID_PUBLIC_KEY=your-vapid-public-key
VAPID_SUBJECT=mailto:admin@yourdomain.com

# Cron endpoint auth secret (used by /api/todos/reminders)
CRON_SECRET=your-random-cron-secret
